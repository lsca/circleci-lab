function sum(x, y) {
  return x + y;
}

function subtra(x, y) {
  return x - y;
}

function multiplication(x,y){
  return x*y;
}

function pow(x){
  return x*x;
} 

module.exports = {
  sum,
  multiplication,
  subtra,
  pow
};